det command run --config-file config-telco.yaml --context . "pip install streamlit-datalist weaviate-client && mkdir -p .streamlit && cp secrets.toml .streamlit/secrets.toml && streamlit run qa_telco.py --server.port 443"
#&& mkdir -p determined && cp -r determined/* determined